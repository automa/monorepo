name: Chromatic (docs)
on:
  push:
    branches: [master]
    paths:
      - .github/workflows/chromatic-docs.yml
      - packages/docs/public/**
      - packages/docs/src/**
      - packages/docs/next.config.js
      - packages/docs/tailwind.config.ts
      - pnpm-lock.yaml
  pull_request:
    branches: [master]
    paths:
      - .github/workflows/chromatic-docs.yml
      - packages/docs/public/**
      - packages/docs/src/**
      - packages/docs/next.config.js
      - packages/docs/tailwind.config.ts
      - pnpm-lock.yaml
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}-${{ github.event_name == 'push' && github.run_number }}
  cancel-in-progress: true
jobs:
  chromatic-docs:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install node
        uses: actions/setup-node@v3
        with:
          node-version: 20
      - name: Install pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      - name: Cache dependencies
        uses: actions/cache@v3
        id: cache
        with:
          path: 'node_modules'
          key: deps-${{ hashFiles('**/pnpm-lock.yaml', '**/schema.prisma') }}
          restore-keys: |
            deps-
      - name: Install dependencies
        run: pnpm install --frozen-lockfile
      - name: Chromatic
        uses: chromaui/action@v1
        with:
          buildScriptName: storybook-build
          token: ${{ secrets.GITHUB_TOKEN }}
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN_DOCS }}
          exitZeroOnChanges: true
          exitOnceUploaded: true
          workingDir: packages/docs
