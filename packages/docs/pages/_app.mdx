import { AnalyticsProvider } from 'analytics';

import 'index.css';

export default function App({ Component, pageProps }) {
  return (
    <AnalyticsProvider>
      <Component {...pageProps} />
    </AnalyticsProvider>
  );
}
