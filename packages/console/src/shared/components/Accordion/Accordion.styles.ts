import * as Accordion from '@radix-ui/react-accordion';

import { tw } from 'theme';

export const Container = tw.div``;

export const Item = tw(Accordion.Item)``;

export const Header = tw(Accordion.Header)``;

export const Trigger = tw(Accordion.Trigger)``;

export const Content = tw(Accordion.Content)``;
