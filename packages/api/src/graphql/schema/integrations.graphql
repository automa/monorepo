enum ProjectProviderType {
  github
  linear
}

type ProjectIntegrationConnection {
  id: Int!
  name: String!
  provider_type: ProjectProviderType!
  config: JSON!
  created_at: DateTime!

  author: User!
}
