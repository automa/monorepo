type Query {
  tasks(org_id: Int!): [Task!]!
}

type Mutation {
  taskCreate(org_id: Int!, input: TaskMessageInput!): Task!
}

type Task {
  id: Int!
  title: String!
  created_at: DateTime!

  org: Org!
  author: User
}

input TaskMessageInput {
  content: String!
}
