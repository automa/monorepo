enum BotType {
  webhook
}

type Query {
  bots(org_id: Int!): [Bot!]!
  publicBots: [PublicBot!]!
  publicBot(org_name: String!, name: String!): PublicBot!
}

type Mutation {
  botCreate(org_id: Int!, input: BotCreateInput!): Bot!
}

type PublicBot {
  id: Int!
  name: String!
  short_description: String!
  description: String
  homepage: String

  org: PublicOrg!
  installation(org_id: Int!): BotInstallation
}

type Bot @inherits(type: "PublicBot") {
  type: BotType!
  webhook_url: String
  published_at: DateTime
  is_published: Boolean!
  created_at: DateTime!

  org: Org!
}

input BotCreateInput {
  name: String!
  short_description: String!
  description: String
  type: BotType!
  webhook_url: String
}
