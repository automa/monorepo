import { Link } from 'react-router-dom';

import { tw } from 'theme';

import Typography from 'shared/components/Typography';

export const Container = tw(Link)`no-underline`;

export const Text = tw(Typography)``;
